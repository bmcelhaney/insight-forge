# Insight Forge Environment Variables
IF_ENV=development
IF_PORT=8080
IF_DUCKDB_PATH=./data/insight-forge.duckdb
IF_LOG_LEVEL=info

# Add source-specific credentials here as needed (never commit real keys)
# WEBFLIS_API_KEY=
# FPDS_API_KEY=
